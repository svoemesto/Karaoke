# Specification Quality Checklist: Бейдж «новые альбомы» в пункте меню «Авторы»

**Purpose**: Проверить полноту и качество спецификации до перехода к планированию.
**Created**: 2026-08-12
**Feature**: [spec.md](./spec.md)

## Content Quality

- [x] Нет implementation details (языков, фреймворков, API) — упоминания конкретных файлов/функций сделаны намеренно как references на существующие паттерны, по которым строится фича (а не как предписания HOW). Сама фича описана через поведение (FR-001..010, SC-001..007).
- [x] Сфокусировано на пользовательской ценности и бизнес-требованиях — User Story описывают, что видит администратор, а не внутренности Vuex.
- [x] Написано для нетехнических стейкхолдеров — терминология «бейдж», «сайдбар», «polling» объяснена в контексте; SQL/Vuex упоминаются только как reference на уже существующий код, а не как требуемая реализация.
- [x] Все обязательные секции заполнены — User Scenarios, Requirements, Success Criteria, Assumptions.

## Requirement Completeness

- [x] Нет [NEEDS CLARIFICATION] маркеров — все требования конкретны и недвусмысленны; единственная потенциально неоднозначная вещь (семантика «новый альбом») уже зафиксирована через `Author.haveNewAlbum` getter.
- [x] Требования тестируемые и однозначные — каждое FR и SC проверяемо вручную или через DevTools/Network.
- [x] Success criteria измеримы — все SC имеют численные пороги (≤ 20 сек, ≤ 100 ms, hex-цвет).
- [x] Success criteria technology-agnostic — SC-001..003 описывают пользовательские действия/видимость; SC-005 ссылается на визуальный атрибут (цвет), а не на CSS-классы.
- [x] Все acceptance scenarios определены — User Story 1 (5 сценариев), 2 (2), 3 (3).
- [x] Edge cases определены — 5 edge cases в соответствующей секции.
- [x] Scope чётко ограничен — Assumptions явно фиксируют, что endpoint без `target`, без DTO в ответе, polling 20 сек, авторы на одной БД.
- [x] Зависимости и предположения идентифицированы — секция Assumptions покрывает: авторизацию, источник истины (`Author.haveNewAlbum`), БД, интервал, размер датасета, связь с существующим фильтром.

## Feature Readiness

- [x] Все FR имеют чёткие acceptance criteria — каждое FR трассируется хотя бы до одного Acceptance Scenario или SC.
- [x] User scenarios покрывают основные потоки — появление бейджа, обновление, консистентность, скрытие при нуле, polling.
- [x] Фича соответствует измеримым outcomes в Success Criteria — User Stories и FR напрямую выражают SC-001..007.
- [x] Нет implementation details, протекающих в спецификацию — упоминания `App.vue`, `Authors/store.js`, `/api/authors/withnewalbumcount` сделаны как «образец» / «endpoint должен существовать», а не как точная инструкция по реализации.

## Notes

- Все проверки пройдены с первого прохода — спецификация готова к `/speckit.plan`.
- При реализации важно НЕ дублировать условие `haveNewAlbum` в SQL руками, а переиспользовать Author.haveNewAlbum getter (см. Edge Cases и FR-003) — иначе фильтр в AuthorsFilterModal и счётчик бейджа могут разойтись.
- Перед коммитом проверить, что polling-таймер действительно очищается в `beforeUnmount` (см. FR-007 и SC-006) — это типичное место утечек при HMR.
- В `App.vue` стили `.chat-nav-badge` и `.songeditor-nav-badge` уже дублируются (см. App.vue:738 и 755) — для `.authors-nav-badge` логично сделать то же самое, а не выносить общий класс (минимизация изменений вне scope фичи).
