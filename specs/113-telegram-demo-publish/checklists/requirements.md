# Specification Quality Checklist: Автопубликация демо-версий песен в Telegram-канал по расписанию

**Purpose**: Валидация полноты и качества спецификации перед переходом к планированию.
**Created**: 2026-07-31
**Feature**: [spec.md](./spec.md)

## Content Quality

- [x] Нет деталей реализации (языки, фреймворки, конкретные API) — упоминания
      `KaraokeProcess*`, `TelegramApiClient`, `RENDER_MP4_DEMO`,
      `KaraokeProperties` — это **ссылки на существующий контекст** (где
      живёт уже работающая функциональность, на которую фича опирается),
      а не предписания «как именно реализовать».
- [x] Фокус на пользовательской ценности и бизнес-потребностях — истории
      описаны с точки зрения администратора и посетителя канала, а не
      разработчика.
- [x] Текст доступен нетехническому стейкхолдеру — терминология
      проекта сохранена, но без погружения в код.
- [x] Все обязательные секции заполнены: User Scenarios & Testing,
      Requirements, Key Entities, Success Criteria, Assumptions, Out of
      Scope, References.

## Requirement Completeness

- [x] **В спеке 0 маркеров [NEEDS CLARIFICATION]** — все вопросы
      (3 на этапе `/speckit.specify` + 5 на этапе `/speckit.clarify`)
      сняты с пользователем и зафиксированы в `## Clarifications`
      спеки (подсекции `### Session 2026-07-31 (initial, from
      /speckit.specify)` и `### Session 2026-07-31 (clarify)`).
- [x] Требования тестируемые и однозначные — каждое FR сформулировано
      как наблюдаемое поведение («система ДОЛЖНА …») с явными
      acceptance-сценариями в User Stories и Edge Cases.
- [x] Success Criteria измеримы — все SC привязаны к наблюдаемым
      метрикам (доля, «ровно одно», «100%», «0%», «без ручных действий»).
- [x] Success Criteria технологически-агностичны — не упоминают
      конкретные библиотеки/СУБД/фреймворки.
- [x] Все acceptance-сценарии определены для P1-историй; для P2 —
      базовые сценарии есть, расширение в детальных сценариях —
      на усмотрение планирования.
- [x] Edge cases определены (нет демо-MP4, превышение лимита,
      неполная готовность, сбой Telegram, исчерпание ретраев,
      параллельный запуск, длинная подпись, пакетная обработка,
      окно 5–10 минут, прошедшая дата/время публикации,
      содержимое песни изменилось после рендера, повторное
      указание даты/времени после очистки).
- [x] Scope чётко ограничен — Telegram only, по-песенный триггер,
      конкретный формат демо (`RENDER_MP4_DEMO`); в Out of Scope явно
      вынесены VK/Boosty/Dzen, косметика UI, изменение формата
      рендера, изменение сущности `Publication`.
- [x] Зависимости и допущения идентифицированы в Assumptions и
      References (cross-references на `telegram-auto-publish.md`,
      `mp4-render.md`, `async-process-queue.md`, `dual-db-sync.md`,
      `sse-notifications.md`, `specs/089`, `specs/101`).

## Feature Readiness

- [x] Все функциональные требования (FR-001 … FR-016, 16 шт.)
      имеют чёткие acceptance-критерии (Given/When/Then в
      User Stories и Edge Cases).
- [x] Пользовательские сценарии покрывают основные потоки:
      автопубликация (P1), сохранение Фазы 1 (P2), видимость
      статусов (P2), ручной триггер через «Опубликовать сейчас»
      (FR-015).
- [x] Фича соответствует измеримым исходам из Success Criteria:
      SC-001…SC-008 напрямую отражают FR-001…FR-016.
- [x] В спецификации нет утечки деталей реализации в формулировках
      требований — имена классов/файлов упоминаются только в
      References как существующий контекст.

## Resolved Clarifications (8 из 8)

| #   | Тема                                | Решение (зафиксировано в spec.md, Clarifications) |
|-----|--------------------------------------|----------------------------------------------------|
| Q1 (specify) | Гранулярность расписания     | По-песенный триггер (собственные дата/время публикации на песне), окно 5–10 минут |
| Q2 (specify) | Режим работы с демо-MP4      | Использовать существующий; при отсутствии — поставить рендер и опубликовать по завершении; при превышении лимита Telegram — перерендерить с уменьшенными параметрами |
| Q3 (specify) | Поведение при заполненном `idTelegramDemo` | Пропускать публикацию (полная замена ручного workflow) |
| Q1 (clarify) | Поведение при дате/времени публикации в прошлом | B — пропускать публикацию (бот считает её «опоздавшей»); администратор может опубликовать через «Опубликовать сейчас» (FR-015) |
| Q2 (clarify) | Как бот определяет «свежесть» существующего демо-MP4 | A — никак, использовать существующий файл без проверки; ответственность за свежесть — на администраторе (FR-003 сценарий 1 упрощён) |
| Q3 (clarify) | Сбой отправки в Telegram: ретрай? | B — ретрай с экспоненциальным backoff до 3 попыток; затем «ошибка отправки» (FR-010) |
| Q4 (clarify) | Что видит админ, пока бот рендерит демо-MP4 | A — явный статус «Рендерится» (новое значение в наборе FR-012); после рендера — «Публикуется», затем «Опубликована» / «Ошибка отправки» |
| Q5 (clarify) | Кнопка «Опубликовать сейчас» | B — кнопка доступна только для песен с пустым `idTelegramDemo`; принудительно триггерит тот же путь, что и наступление даты/времени (FR-015, FR-016) |

## Notes

- Все 8 уточнений (3 на этапе specify + 5 на этапе clarify) сняты до
  перехода к `/speckit.plan` — план может стартовать без
  дополнительных блокеров.
- Исторический контекст уже существующих родственных фич —
  `specs/089-auto-news-song-release` и `specs/101-song-news-flag` —
  использован как источник переиспользуемых паттернов (определение
  «полной готовности песни», плановый триггер со скользящим окном,
  идемпотентность через состояние, а не через таблицу учёта).
- Фаза 1 фичи (`docs/features/telegram-auto-publish.md` —
  парсинг уже вышедших постов) явно сохраняется (FR-009) и
  описана как страховочный/исключительный сценарий, что отражено
  в SC-002 и SC-005.
- Спецификация НЕ вводит новой сущности `Publication`-уровня и
  НЕ модифицирует существующую `model/Publication.kt` — расписание
  живёт на уровне самой песни (FR-002). Это упрощает миграцию
  существующих данных и сводит к минимуму изменения в админке.
- Q2 (clarify) упростил FR-003 сценарий 1: убрана проверка
  «актуальности» — не требуется новое поле `demo_render_source_hash`
  в `Settings`. Это **снижает** объём миграций схемы.
- Q4 (clarify) расширил набор состояний публикации в FR-012
  с 4 до 6 значений: «запланирована» / «рендерится» /
  «публикуется» / «опубликована» / «ошибка отправки» /
  «отменена». Эти 6 значений должны быть представлены в
  `webvue3`-UI (SC-006).
- Q5 (clarify) добавил FR-015/FR-016 — кнопка «Опубликовать
  сейчас» в админке. Это **расширяет** UI обязательства фичи
  (одна новая кнопка в карточке песни), но не вводит новых
  сущностей.
- Согласно AGENTS.md (Pass 33, 2026-07-29), переход в код
  должен идти через: `./tools/reserve-branch-number.sh
  telegram-demo-publish` → feature-ветка `113-telegram-demo-publish`
  → PR с CI 7/7 SUCCESS. Текущее состояние в git: только
  untracked-каталог `specs/113-telegram-demo-publish/`, никаких
  коммитов в этой сессии не сделано.
