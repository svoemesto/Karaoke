## Question

Где в коде фронта живёт существующий бейдж счётчика KaraokeProcess
(правый нижний угол кнопки Старт/Стоп в админке, компонент «Песни»),
как он получает данные о количестве процессов, какие стили/цвета
используются в админке, какой фреймворк используется (Vue/React/Svelte),
какой store/state manager.

Нужно для создания второго бейджа — синего, в правом верхнем углу той же
кнопки, отображающего размер пула HealthReport.

## Investigation scope

- `webvue3/` и/или `karaoke-public/` — найти компонент «Песни» в админке
  (AdminSongs, SongList, SongsAdmin — типичные имена).
- Найти компонент-владелец бейджа KaraokeProcess — где сейчас
  отображается счётчик, как он стилизован (CSS-классы, цвета), как
  привязан к store.
- Конвенция синего цвета в проекте: проверить design tokens, theme,
  CSS-переменные.

## Outputs

- Полные пути к компонентам и store (с line numbers).
- Цитата существующего бейджа KaraokeProcess (template + style + script).
- Цитата store/selectors, откуда берётся `processCount`.
- Список доступных цветов/blue-shades в design tokens (если есть).
- Подтверждение фреймворка (Vue 3 / Pinia / etc).

## Out of scope (Q2)

- Реализация бейджа — это Q4.
- Подписка на изменения пула (используем существующий механизм
  подписки, если есть; иначе короткий poll — это решается в Q4).

Тип: [wayfinder:research]
Блокирует: Q4 (бейдж)
Заблокирован: — (можно стартовать сразу)