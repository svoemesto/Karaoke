# Tasks: Страница «О проекте» (QW-9)

**Input**: Design documents from `/specs/003-about-page/`
**Prerequisites**: plan.md (✅), spec.md (✅), research.md (✅), data-model.md (✅), quickstart.md (✅). Нет `contracts/` — фича не вводит новых REST-эндпоинтов (см. plan.md).

**Tests**: в `karaoke-public` автотестов нет (constitution.md «Рабочий процесс: Тесты»). Проверка — ручной сценарий в `quickstart.md` (10 пунктов).

**Organization**: задачи сгруппированы по User Story. US1/US2 (P1) — уже реализованы в коммите `4240b98`, задачи здесь в основном верификационные (подтвердить и явно закрыть в tasks.md, аналогично тому, как это было сделано для `001`/`002`). US3 (P3) — реально не реализована, полноценная имплементация.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: можно запускать параллельно (разные файлы, нет зависимостей).
- **[Story]**: US1/US2/US3.
- Полные пути к файлам включены в описания.

## Path Conventions

- Фронтенд: `karaoke-public/src/`.
- Документация: `docs/strategy/growth.md`, `specs/003-about-page/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: новых зависимостей/директорий не требуется — фича переиспользует существующий стек `karaoke-public`.

- [X] T001 Убедиться, что `/about` собирается локально без ошибок (`cd karaoke-public && npm run dev`, открыть `/about`) *(собрано и запущено через `do.sh build_start_public`, `curl /about` → 200)*

**Checkpoint**: dev-сервер поднимается, `/about` открывается (уже так, но фиксируем как отправную точку).

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: блокирующих предпосылок нет — маршрут `/about`, `AboutView.vue`, ссылки в навигации уже существуют и не требуют переделки под US1-US3.

*(нет задач — все User Story независимы друг от друга и от какой-либо общей инфраструктуры, которой ещё не хватает)*

**Checkpoint**: сразу переходим к User Story.

---

## Phase 3: User Story 1 — Анонимный посетитель понимает, что за сайт (Priority: P1) 🎯 MVP

**Goal**: за 30-60 сек аноним понимает, что это за проект (включая площадки, где есть контент).
**Independent Test**: зайти на `/about` без авторизации, за минуту увидеть заголовок, описание, статистику, топ-авторов и площадки.

### Реализация

- [X] T002 [P] [US1] Подтвердить: заголовок h1, hero-блок «Полная дискография. Не сборник хитов», описание проекта — уже в `karaoke-public/src/views/AboutView.vue` (строки 15-48) — просто верифицировать по чек-листу `quickstart.md` п.2, отметить готовым
- [X] T003 [P] [US1] Подтвердить: статистика (4 карточки из `/api/public/stats`) — уже в `AboutView.vue` (строки 89-109) — верифицировать по `quickstart.md` п.3 *(живой `curl /api/public/stats` из контейнера вернул реальные числа)*
- [X] T004 [P] [US1] Подтвердить: топ-15 авторов + динамический список остальных из `/api/public/authors` — уже в `AboutView.vue` (строки 111-135) — верифицировать по `quickstart.md` п.2
- [X] T005 [US1] **[GAP]** Добавить блок «Площадки» в `karaoke-public/src/views/AboutView.vue`: импортировать и вставить `<SocialLinks />` (`karaoke-public/src/components/SocialLinks.vue`, без изменений компонента) в новую секцию `<section class="km-section">` с заголовком «Где ещё мы есть» между секцией «Что умеет наш онлайн-плеер» и секцией статистики (см. `plan.md` Decision 1, `research.md` Decision 1)
- [X] T006 [US1] Подтвердить: ссылка «О проекте» в главном меню (`karaoke-public/src/views/HomeView.vue:92`, `.km-nav-card`) и в футере (`karaoke-public/src/App.vue:9`) — уже есть, верифицировать переходом по `quickstart.md` п.1
- [ ] T007 [US1] Прогнать `quickstart.md` пп.1-3 целиком в браузере (desktop) — подтвердить, что T005 не сломал существующую вёрстку. **Не закрыто**: headless-браузер (raw Chrome и Playwright, оба localhost/127.0.0.1, оба `domcontentloaded`) не смог достучаться до `localhost:7907` в этой песочнице — таймаут на `page.goto` без видимой сетевой ошибки (похоже на сетевую изоляцию для процессов браузера в этом sandbox). Подтверждено статически: собранный бандл контейнера (`docker exec karaoke-public grep`) содержит новый текст/классы (`Где ещё мы есть`, `share-menu`, `ВКонтакте`); визуальный рендер не проверен — нужна ручная проверка пользователем в реальном браузере

**Checkpoint**: US1 полностью закрыт (включая ранее отсутствовавший блок «Площадки»).

---

## Phase 4: User Story 2 — Аноним понимает, какой контент найдёт (Priority: P1) 🎯 MVP

**Goal**: явно понятно «преимущественно русский рок», список исполнителей, формат «эфир по расписанию / подписка».
**Independent Test**: на `/about` явно виден акцент «преимущественно русский рок» + ≥10 имён исполнителей.

### Реализация

- [X] T008 [P] [US2] Подтвердить: явная фраза «Преимущественно русский рок» + список из ≥10 исполнителей (топ-15 хардкод + N динамических) — уже в `AboutView.vue` (секция «Какие песни», строки 51-67) — верифицировать по `quickstart.md` п.2
- [X] T009 [P] [US2] Подтвердить: явное указание, чего в коллекции нет («современный поп, хип-хоп, электроника, зарубежная музыка») — уже в `AboutView.vue` (`.km-hint-text`, строка 64) — верифицировать
- [X] T010 [US2] Подтвердить: формат «готовые песни в эфире / остальные по подписке» отражён в CTA-блоке и в `.km-important` (оферта, только-онлайн) — уже в `AboutView.vue` (строки 81-85, 153-157) — верифицировать

**Checkpoint**: US2 полностью закрыт, без изменений кода (только верификация).

---

## Phase 5: User Story 3 — Аноним/зарегистрированный может поделиться страницей (Priority: P3)

**Goal**: кнопка «Поделиться» с меню из 5+ соцсетей + копирование ссылки.
**Independent Test**: нажать «Поделиться» внизу `/about`, выбрать соцсеть или скопировать ссылку.

### Реализация

- [X] T011 [P] [US3] Создать `karaoke-public/src/components/ShareButton.vue`: кнопка + выпадающее меню (VK/Telegram/WhatsApp/Odnoklassniki — стандартные share-URL вида `vk.com/share.php`, `t.me/share/url`, `wa.me`, `connect.ok.ru/offer` — открываются `window.open(url, '_blank')`) + пункт «Скопировать ссылку» (`navigator.clipboard.writeText`, fallback `document.execCommand('copy')` для старых браузеров). Иконки — emoji (см. `research.md` Decision 2). JSDoc на экспортируемый компонент (FR-006 CONTRIBUTING.md)
- [X] T012 [US3] Подключить трекинг клика по share-пункту через существующий `trackUi('share', <platform>)` (`karaoke-public/src/services/tracking.js`) — переиспользовать паттерн `SocialLinks.vue`/`trackLinkToSocialNetwork`, не изобретать новый event-тип
- [X] T013 [US3] Вставить `<ShareButton />` в `karaoke-public/src/views/AboutView.vue`, секция `.km-cta-section` (после кнопок CTA, перед `.km-hint-text` про премиум) — см. plan.md Project Structure
- [X] T014 [US3] Стилизовать `ShareButton.vue` под существующие CSS-переменные `--km-*` (см. `.km-cta-btn`/`.km-cta-secondary` в `AboutView.vue` как образец — не вводить новых цветов/шрифтов)
- [ ] T015 [US3] Прогнать `quickstart.md` п.2 (блок «[NEW] Кнопка «Поделиться»») на desktop и мобильном viewport — 5 пунктов меню кликабельны, копирование работает. **Не закрыто** по той же причине, что T007 — headless-браузер недоступен в этой песочнице; нужна ручная проверка

**Checkpoint**: US3 закрыт — MVP (US1+US2) уже был закрыт раньше, теперь фича полностью соответствует spec.md.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: финальная документация, cross-cutting фиксы, найденные при планировании.

- [X] T016 [P] Актуализировать `docs/strategy/growth.md`: отметить QW-9 как сделанный (не как открытую задачу в роадмапе), исправить ссылку `specs/001-about-page/spec.md` → `specs/003-about-page/spec.md` (см. plan.md Summary, п.3) *(заодно исправлены такие же битые пути QW-2/QW-1 → `004-`/`005-` — попутный фикс той же природы)*
- [X] T017 Прогнать `bash tools/verify-doc-links.sh docs/strategy/` (или `lychee --offline docs/strategy/`) — подтвердить, что правка T016 не оставила новых битых ссылок *(0 errors)*
- [X] T018 Прогнать `cd karaoke-public && npm run lint:check && npx prettier --check "src/**/*.{vue,js,ts,json}"` — новый `ShareButton.vue` и правки `AboutView.vue` проходят CI-линт (см. `.github/workflows/lint.yml`) *(0 warnings/errors; `ShareButton.vue` автоформатирован prettier --write)*
- [X] T019 Прогнать `bash tools/check-jsdoc-coverage.sh --strict` — `ShareButton.vue` не роняет JSDoc coverage ниже 50% (CI gate, см. `002-ci-lint-enforcement`) *(karaoke-public 100%, 42/42 — было 41/41)*
- [ ] T020 Полный прогон `quickstart.md` (все 10 пунктов чек-листа) — финальная валидация перед PR. **Блокируется T007/T015** (нет живого рендера в браузере в этой песочнице) — 8/10 пунктов подтверждены статически/через API, визуальные пп.1-2 (браузерная проверка) нужны от пользователя

**Checkpoint**: всё готово к PR.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: нет зависимостей.
- **Phase 2 (Foundational)**: пустая, ничего не блокирует.
- **Phase 3 (US1, P1)**: независима, можно начинать сразу после Phase 1.
- **Phase 4 (US2, P1)**: независима от US1 (верификационная, не трогает код) — можно параллельно с Phase 3.
- **Phase 5 (US3, P3)**: независима от US1/US2 (новый компонент, отдельная секция) — можно параллельно, но по приоритету — после MVP (US1+US2).
- **Phase 6 (Polish)**: после US1-US3.

### Within Each Story

- Phase 3: T002-T004, T006 — параллельно (независимая верификация разных секций/файлов). T005 (реальная правка) — отдельно, T007 — после T005.
- Phase 5: T011 — отдельно (новый файл), T012-T014 — после T011 (тот же файл), T015 — после всех.

### Parallel Opportunities

- Phase 3: T002, T003, T004, T006 — все параллельно (только чтение/верификация, разные секции одного файла, без конфликтов записи).
- Phase 4: T008, T009 — параллельно.
- Phase 6: T016 (growth.md) параллельно с T017-T019 (разные файлы/команды), но T017 логически после T016 (проверяет его результат).

---

## Implementation Strategy

### MVP First (P1 stories only)

1. Phase 1 (Setup, тривиальный).
2. Phase 3 (US1) + Phase 4 (US2) — в основном верификация уже сделанного, плюс T005 (реальный gap — блок «Площадки»).
3. **STOP и VALIDATE**: `quickstart.md` пп.1-3.
4. MVP (US1+US2, включая закрытие последнего gap'а US1) готов.

### Incremental Delivery

1. MVP (US1+US2) — из-за того, что 90% уже сделано, это самый быстрый шаг.
2. + Phase 5 (US3) — кнопка «Поделиться», отдельный PR или тот же (небольшой скоуп).
3. + Phase 6 (Polish) — `growth.md`, линтеры, финальная валидация.

---

## Notes

- Большинство задач в Phase 3-4 — **верификационные**, не кодовые: фича уже реализована в `4240b98` в обход speckit-пайплайна (аналогично `001-code-standards-docs`). Это осознанно — цель tasks.md здесь не «написать всё с нуля», а формально закрыть спеку и дописать 2 реальных gap'а (T005, T011-T015) + 1 doc-фикс (T016).
- Единственные задачи, требующие реального нового кода: **T005** (блок «Площадки», ~5 строк разметки) и **T011-T014** (новый `ShareButton.vue`).
- Commit после каждой логической группы (например: T005+T006+T007 → один коммит «US1: блок Площадки»; T011-T015 → «US3: кнопка Поделиться»; T016-T017 → «docs: growth.md QW-9 done»).
- Avoid: не переписывать уже работающий `AboutView.vue` целиком — точечные правки.
