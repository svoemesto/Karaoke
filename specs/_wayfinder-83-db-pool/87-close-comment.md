# OpenProject #87 — Close Reason (Pass 372)

> **Status**: ✅ RESOLVED 2026-09-13.
> **Закрыт как**: «не нужно после фиксов A+B».

## Причина закрытия

Изнатльный план был — согласовать с владельцем **целевые метрики** для проверки поведения
приоритезированной очереди: latency budget, поведение при уходе/возврате, UX-loading,
возможен ли bulk HR endpoint.

После фиксов A+B (#89 + #90) **приоритезированная очередь не реализуется** (см. close для
#86), и сами метрики **не нужны** — мы устранили корневую причину.

Если в будущем понадобится мониторинг pg_stat_activity для предотвращения подобных проблем —
это отдельная задача (Pass 372+), **не привязанная к #83**.

## Метрики, которые были бы полезны (но сейчас не приоритетны)

- Количество одновременных JDBC connections от karaoke-app / karaoke-web (через `pg_stat_activity` scrape).
- Histogram `ol-2-thread-N` (cacheFillerExecutor) — для подтверждения, что executor не превышает 16.
- Гистограмма latency HR-запросов (`/api/song/healthReportList`).

Если владелец захочет — может создать отдельный тикет.

— close для #87