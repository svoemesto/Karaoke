# DEPRECATED — deploy/web-server-deploy

> **Статус**: DEPRECATED (2026-09-23, wayfinder #165 / спека #178).
> **НЕ ИСПОЛЬЗОВАТЬ** для новых изменений.

Это устаревшая копия deploy для **двухсерверной** топологии
(прод-сервер `188.119.64.111` + отдельный storage-сервер `89.125.103.63`).
Она рассинхронизирована с реальным прод-конфигом (например, файл `80to8897`
здесь — старая версия без HTTP-кэша `/minio/`).

**Актуально**: [`deploy/prod-single-host/`](../prod-single-host/README.md) —
канон однохостового деплоя (прод + локальный MinIO на `188.127.240.124`),
собранный из реального `/root/Karaoke/deploy`.

Оставлено для истории; будет удалено отдельной задачей
(см. `specs/439-migration-one-server-exec/`, Not yet specified).
